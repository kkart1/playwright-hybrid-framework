import { test, expect } from '../fixtures/baseTest';

test('@smoke Admin dashboard test', async ({ adminPage }) => {

  // Example navigation (replace with real protected page later)
  await adminPage.navigate();

  // Example assertion
  await expect(true).toBeTruthy();

});